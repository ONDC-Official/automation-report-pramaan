const { issue } = require("./issue");
const { issue_status } = require("./issueStatus");
const { issue_close } = require("./issueClose");


module.exports = {
    issue,
    issue_status,
    issue_close
}