const Mocha = require('mocha');
const { expect } = require('chai');

function paymentsCommonTests (message,messageTestSuite){
    try {
        
    } catch (error) {
        console.log(err);
        return(error);
        
    }
}

module.exports = {
    paymentsCommonTests
}
